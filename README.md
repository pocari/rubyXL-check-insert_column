```
git clone https://github.com/pocari/rubyXL-check-insert_column.git
bundle config set path 'vendor/bundle' --local
bundle install
bundle exec ruby main.rb

# Open the output.xlsx generated by the command execution
```